# 多灯连接功能使用指南

## 🎉 功能已完成

多灯连接功能已实现，支持同时管理多个小米显示器挂灯和米家台灯。

---

## ✨ 主要功能

### 1. 多设备支持
- ✅ 支持配置多个设备
- ✅ 所有设备响应系统事件（启动/关闭/休眠/快捷键）
- ✅ Per-device 配置项（每个设备独立设置）

### 2. 自动配置迁移
- ✅ 旧版配置自动升级到新格式
- ✅ 无需手动操作，完全透明

### 3. 广播控制
- ✅ **快捷键**：控制所有在线设备
- ✅ **启动开灯**：只开启启用该功能的设备
- ✅ **关闭关灯**：只关闭启用该功能的设备
- ✅ **休眠唤醒**：保存和恢复每个设备状态

---

## 📖 使用方法

### 方法 1: 手动编辑配置文件（推荐）

**步骤**：

1. 关闭应用程序

2. 打开配置文件：
   ```
   %APPDATA%\MiMonitorLightTray\config.json
   ```
   完整路径：`C:\Users\你的用户名\AppData\Roaming\MiMonitorLightTray\config.json`

3. 在 `"devices"` 数组中添加设备：

```json
{
  "devices": [
    {
      "id": "temp_abc12345",
      "ip": "192.168.31.140",
      "token": "你的32位token",
      "name": "显示器挂灯",
      "model": "xiaomi.light.bar2",
      "device_id": 0,
      "enable_miot_for_unknown": false,
      "power_on_at_startup": true,
      "power_off_at_exit": false,
      "power_off_on_monitor_sleep": true,
      "power_off_on_system_suspend": false,
      "power_on_on_system_resume": true
    },
    {
      "id": "temp_def67890",
      "ip": "192.168.31.141",
      "token": "另一个32位token",
      "name": "书桌台灯",
      "model": "yeelink.light.lamp22",
      "device_id": 0,
      "enable_miot_for_unknown": false,
      "power_on_at_startup": false,
      "power_off_at_exit": true,
      "power_off_on_monitor_sleep": false,
      "power_off_on_system_suspend": true,
      "power_on_on_system_resume": false
    }
  ],
  "active_device_id": "ALL",
  ...
}
```

4. 保存文件并重启应用

**说明**：
- `id`: 填 `temp_` 加任意8位字母数字，首次连接后会自动更新
- `ip`: 设备的局域网 IP 地址
- `token`: 32 位十六进制 token（可用云端导入获取）
- `name`: 自定义设备名称
- `model`: 设备型号（留空自动识别）
- `device_id`: 填 `0`，首次连接后自动获取
- `power_on_at_startup`: 程序启动时开灯
- `power_off_at_exit`: 程序退出时关灯
- `power_off_on_monitor_sleep`: 显示器休眠时关灯
- `power_off_on_system_suspend`: 系统休眠时关灯
- `power_on_on_system_resume`: 系统唤醒时开灯

### 方法 2: 使用云端导入

1. 打开应用 → 设置
2. 点击「自动获取」或「云端导入」
3. 登录小米账号
4. 选择第一个设备并保存
5. 重复步骤 2-4 添加其他设备

**提示**：目前云端导入一次只能选择一个设备，需要多次操作。

---

## 🎮 设备控制行为

### 快捷键
按下快捷键后，**所有在线设备**同时响应：
- 亮度增加 → 所有灯增加亮度
- 亮度降低 → 所有灯降低亮度
- 色温增加 → 所有灯色温增加
- 色温降低 → 所有灯色温降低

### 系统事件
根据每个设备的配置项独立控制：

| 事件 | 行为 |
|------|------|
| 程序启动 | 只开启 `power_on_at_startup=true` 的设备 |
| 程序退出 | 只关闭 `power_off_at_exit=true` 的设备 |
| 显示器休眠 | 关闭 `power_off_on_monitor_sleep=true` 的设备 |
| 显示器唤醒 | 恢复之前关闭的设备 |
| 系统休眠 | 关闭 `power_off_on_system_suspend=true` 的设备 |
| 系统唤醒 | 开启 `power_on_on_system_resume=true` 的设备 |

### UI 控制
- **Flyout 窗口**：当前控制第一个设备
- **托盘菜单**：开关切换影响第一个设备

---

## 🔍 常见场景示例

### 场景 1: 显示器挂灯 + 书桌台灯

**需求**：
- 显示器挂灯随显示器休眠开关
- 书桌台灯只在关闭程序时关灯
- 快捷键同时控制两者

**配置**：
```json
{
  "devices": [
    {
      "name": "显示器挂灯",
      "ip": "192.168.31.140",
      "power_off_on_monitor_sleep": true,
      ...
    },
    {
      "name": "书桌台灯",
      "ip": "192.168.31.141",
      "power_off_at_exit": true,
      ...
    }
  ]
}
```

### 场景 2: 多个显示器挂灯

**需求**：
- 3 个显示器各配一个挂灯
- 全部同步控制

**配置**：
```json
{
  "devices": [
    {"name": "左显示器挂灯", "ip": "192.168.31.140", ...},
    {"name": "中显示器挂灯", "ip": "192.168.31.141", ...},
    {"name": "右显示器挂灯", "ip": "192.168.31.142", ...}
  ]
}
```

所有设备默认响应快捷键和系统事件。

### 场景 3: 工作灯 + 氛围灯分离控制

**需求**：
- 工作灯（显示器挂灯）随显示器休眠开关
- 氛围灯（台灯）独立控制，不受影响

**配置**：
```json
{
  "devices": [
    {
      "name": "工作灯",
      "power_on_at_startup": true,
      "power_off_on_monitor_sleep": true,
      ...
    },
    {
      "name": "氛围灯",
      "power_on_at_startup": false,
      "power_off_on_monitor_sleep": false,
      ...
    }
  ]
}
```

---

## ❓ 常见问题

### Q1: 如何获取设备的 IP 和 Token？

**方法 1: 米家 App**
1. 打开米家 App
2. 进入设备详情 → 右上角"…" → 设备信息
3. IP 地址会显示在底部

**方法 2: 路由器管理界面**
- 查看已连接设备列表

**获取 Token**：
- 使用应用内的「自动获取」或「云端导入」功能
- 或使用 [Xiaomi-cloud-tokens-extractor](https://github.com/PiotrMachowski/Xiaomi-cloud-tokens-extractor) 工具

### Q2: 添加设备后不生效？

**检查清单**：
1. ✅ 配置文件 JSON 格式正确（可用在线 JSON 验证器检查）
2. ✅ 重启了应用
3. ✅ 设备 IP 地址正确且在线
4. ✅ Token 是 32 位十六进制字符串

### Q3: 某个设备离线会影响其他设备吗？

**不会**。系统会自动跳过离线设备，继续控制在线设备。

### Q4: 如何临时禁用某个设备？

**方法 1**：在配置文件中删除该设备条目

**方法 2**：将所有开关设为 `false`：
```json
{
  "power_on_at_startup": false,
  "power_off_at_exit": false,
  "power_off_on_monitor_sleep": false,
  "power_off_on_system_suspend": false,
  "power_on_on_system_resume": false
}
```

设备仍会响应快捷键，但不响应系统事件。

### Q5: 支持哪些设备型号？

支持所有基于 Yeelink/MIoT 协议的米家灯具：
- ✅ 小米显示器挂灯系列（xiaomi.light.bar\*）
- ✅ 米家台灯系列（yeelink.light.lamp\*）
- ✅ 米家吸顶灯系列（yeelink.light.ceil\*）
- ✅ 其他 Yeelink 灯具

### Q6: 配置文件格式错误怎么办？

**恢复方法**：
1. 删除配置文件
2. 重启应用，会生成新的默认配置
3. 重新添加设备

**备份建议**：修改前先备份 `config.json`

---

## 🐛 故障排除

### 问题：设备无法连接

**解决方法**：
1. 确认设备已连接到同一局域网
2. Ping 测试：`ping 设备IP`
3. 检查路由器是否启用了 AP 隔离
4. 尝试在米家 App 中重置设备

### 问题：快捷键不生效

**解决方法**：
1. 检查快捷键是否与其他程序冲突
2. 在设置中重新绑定快捷键
3. 以管理员权限运行应用

### 问题：显示器休眠功能不工作

**解决方法**：
1. 确认系统支持显示器休眠事件
2. Modern Standby 系统需要应用版本 v1.4.3+
3. 检查设备的 `power_off_on_monitor_sleep` 配置项

---

## 📝 配置文件字段说明

### 设备配置 (DeviceConfig)

```json
{
  "id": "设备唯一标识符",
  "ip": "设备IP地址",
  "token": "32位miio token",
  "name": "设备显示名称",
  "model": "设备型号（可选，留空自动识别）",
  "device_id": "硬件设备ID（首次连接自动获取）",
  "enable_miot_for_unknown": "未知型号使用MIoT协议（实验性）",
  "power_on_at_startup": "程序启动时开灯",
  "power_off_at_exit": "程序退出时关灯",
  "power_off_on_monitor_sleep": "显示器休眠时关灯",
  "power_off_on_system_suspend": "系统休眠时关灯",
  "power_on_on_system_resume": "系统唤醒时开灯"
}
```

### 全局配置 (AppConfig)

```json
{
  "devices": [设备列表],
  "active_device_id": "当前活动设备ID（UI使用）",
  "widget": {桌面小部件配置},
  "hotkey": {快捷键配置},
  "auto_check_update": "自动检查更新"
}
```

---

## 🚀 下一步改进（可选）

以下为可选的 UI 增强功能，不影响当前使用：

1. **设备列表 UI**：在设置窗口中图形化管理设备
2. **设备选择器**：在 Flyout 中选择控制哪个设备
3. **云端多选**：一次导入多个设备

这些功能可以作为后续版本改进，当前版本的手动配置方式已完全满足使用需求。

---

## 💡 技巧和建议

### 建议 1: 设备命名规范
使用描述性名称，方便识别：
- ✅ "左显示器挂灯"、"右显示器挂灯"
- ✅ "工作台灯"、"氛围台灯"
- ❌ "Light 1"、"Light 2"

### 建议 2: 配置文件备份
修改配置前先备份：
```powershell
copy %APPDATA%\MiMonitorLightTray\config.json config.json.backup
```

### 建议 3: 使用静态 IP
为设备分配静态 IP 或 DHCP 保留，避免 IP 变更。

### 建议 4: 分组控制
通过配置项实现逻辑分组：
- 工作灯组：启用 `power_off_on_monitor_sleep`
- 氛围灯组：不启用任何自动开关
- 全局组：所有灯响应快捷键

---

**版本**: v1.5.0+
**最后更新**: 2026-07-25
**反馈**: 如有问题或建议，请提交 GitHub Issue
